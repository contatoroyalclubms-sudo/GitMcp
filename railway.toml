[build]
builder = "nixpacks"
buildCommand = "cd frontend && npm install --legacy-peer-deps && npm run build"

[deploy]
startCommand = "npx serve -s frontend/dist -l tcp://0.0.0.0:$PORT"

[[services]]
name = "web"
